# Scripts

Planned command-line helpers:

- `import_hands.py`: import exported GG files into local storage.
- `run_report.py`: generate the latest Markdown review report.
- `validate_fixtures.py`: compare parser output with expected fixture JSON.

Do not add scripts that read a running poker client or provide live decisions.
